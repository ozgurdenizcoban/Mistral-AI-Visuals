kind = "web"
previewPath = "/"
title = "Apex — TUS Zirvesi"
version = "1.0.0"
id = "artifacts/apextus"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23480

[services.development]
run = "pnpm --filter @workspace/apextus run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/apextus", "run", "build" ]
publicDir = "artifacts/apextus/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23480"
BASE_PATH = "/"
